# 业务流程索引

本文档提供 Import_Bill_To_Notion 项目所有业务流程的快速索引。

## 目录

- [用户认证流程](#用户认证流程)
- [账单导入流程](#账单导入流程)
- [财务复盘流程](#财务复盘流程)
- [管理后台流程](#管理后台流程)

---

## 用户认证流程

### 1. 用户注册

**入口**: `POST /api/auth/register`

**代码位置**: `web_service/routes/auth.py`

**流程图**:
```
用户填写注册表单
      |
      v
验证输入（用户名格式、邮箱、密码强度）
      |
      v
检查用户名是否已存在
      |
      v
检查邮箱是否已存在
      |
      v
生成密码哈希
      |
      v
创建用户记录
      |
      v
生成访问令牌和刷新令牌
      |
      v
创建会话记录
      |
      v
返回令牌和用户信息
```

**详细文档**: [业务流程详解](./业务流程详解.md#用户注册)

---

### 2. 用户登录

**入口**: `POST /api/auth/login`

**代码位置**: `web_service/routes/auth.py:48-95`

**流程图**:
```
用户提交用户名和密码
      |
      v
检查账户是否被锁定
      |
      v
验证用户名和密码
      |
      v
+-----失败-----+
|              |
v              v
记录失败次数   生成访问令牌
      |              |
      v              v
达到5次？      清除失败记录
      |              |
      v              v
  锁定账户      创建会话记录
  (30分钟)          |
                    v
              返回令牌和用户信息
```

**详细文档**: [业务流程详解](./业务流程详解.md#用户登录)

---

### 3. 令牌刷新

**入口**: `POST /api/auth/refresh`

**代码位置**: `web_service/routes/auth.py:98-125`

**流程图**:
```
客户端提交刷新令牌
      |
      v
验证刷新令牌有效性
      |
      v
提取用户信息
      |
      v
生成新的访问令牌
      |
      v
（可选）生成新的刷新令牌
      |
      v
返回新令牌
```

---

## 账单导入流程

### 4. 文件上传与导入

**入口**: `POST /api/upload`

**代码位置**: `web_service/routes/upload.py:42-78`

**流程图**:
```
用户选择文件上传
      |
      v
文件大小和格式验证
      |
      v
保存文件到服务器
      |
      v
创建上传记录（UserUpload）
      |
      v
+-----立即导入-----+
|                 |
v                 v
平台检测      等待调度器
      |                 |
      v                 v
选择解析器        (未实现)
      |
      v
解析CSV/Excel
      |
      v
转换为Notion格式
      |
      v
过滤"不计收支"记录
      |
      v
批量导入Notion
      |
      v
创建导入历史记录
      |
      v
返回导入结果
```

**详细文档**: [业务流程详解](./业务流程详解.md#账单导入)

---

### 5. 平台检测

**代码位置**: `parsers/__init__.py:15-80`

**流程图**:
```
读取文件前20行
      |
      v
+----Excel文件?----+
|                 |
Yes                No
|                 |
v                 v
pandas读取      尝试多种编码读取
      |                 |
      v                 v
转换为文本    检查平台关键词
      |
      +-------+-------+
              |
              v
     +--------+--------+
     |                 |
包含微信关键词?    包含支付宝关键词?
     |                 |
     v                 v
  WeChatPay         Alipay
     |                 |
     +--------+--------+
              |
              v
       包含银联关键词?
              |
              v
          UnionPay
```

---

### 6. 数据解析与转换

**代码位置**: `parsers/base_parser.py`, `parsers/alipay_parser.py`

**流程图**:
```
解析器初始化
      |
      v
查找表头行
      |
      v
读取CSV/Excel数据
      |
      v
应用列映射
      |
      v
清洗金额字段
      |
      v
标准化日期格式
      |
      v
逐行转换为Notion格式
      |
      +----"不计收支"?----+
    |                    |
    v                    v
  跳过              转换为Notion属性
```

---

## 财务复盘流程

### 7. 复盘生成

**入口**: `POST /api/review/generate`

**代码位置**: `src/review_service.py:1401-1485`

**流程图**:
```
用户选择复盘类型和周期
      |
      v
检查复盘数据库配置
      |
      v
计算日期范围
      |
      v
+----日期范围>90天?----+
|                     |
Yes                   No
|                     |
v                     v
分批查询(30天/批)   直接查询
      |                     |
      +-------+-------------+
              |
              v
    获取收支数据库交易记录
              |
              v
        计算汇总数据
        (收入、支出、结余)
              |
              v
        按分类聚合统计
              |
              v
        生成分类TOP5
              |
              v
        +---有模板?---+
        |            |
        v            v
    使用模板      使用默认格式
        |            |
        v            v
    填充模板数据  创建基础页面
        |            |
        +-------+----+
                |
                v
          创建Notion页面
                |
                v
          返回页面URL
```

**详细文档**: [业务流程详解](./业务流程详解.md#财务复盘)

---

### 8. 复盘预览

**入口**: `GET /api/review/preview`

**代码位置**: `web_service/routes/review.py`

**流程图**:
```
用户选择复盘类型和周期
      |
      v
查询交易数据
      |
      v
计算汇总和分类
      |
      v
返回预览数据（不创建页面）
```

---

## 管理后台流程

### 9. 用户管理（管理员）

**入口**: `GET/POST/PATCH/DELETE /api/admin/users`

**代码位置**: `web_service/routes/admin.py`

**流程图**:
```
管理员登录
      |
      v
验证超级用户权限
      |
      v
+---操作类型---+
|   |   |   |
列表 创建 更新 删除
|   |   |   |
v   v   v   v
查询 创建 更新 删除
用户 用户 用户 用户
|   |   |   |
v   v   v   v
返回 结果
```

---

### 10. 系统配置管理

**入口**: `GET/PATCH /api/admin/settings`

**代码位置**: `web_service/routes/admin.py:180-220`

**流程图**:
```
管理员请求
      |
      v
验证权限
      |
      v
+---操作类型---+
|             |
获取          更新
|             |
v             v
读取          更新
系统          系统
设置          设置
|             |
v             v
返回          返回
配置          结果
```

---

### 11. 审计日志查询

**入口**: `GET /api/admin/audit-logs`

**代码位置**: `web_service/routes/admin.py:140-175`

**流程图**:
```
管理员请求
      |
      v
验证权限
      |
      v
应用过滤条件
(用户、操作类型、时间)
      |
      v
查询审计日志
      |
      v
关联用户信息
      |
      v
分页返回结果
```

---

## 配置验证流程

### 12. Notion配置验证

**入口**: `GET /api/user/notion-config/verify-all`

**代码位置**: `web_service/routes/users.py:115-165`

**流程图**:
```
用户点击验证配置
      |
      v
+---步骤1: 验证API密钥---+
|                       |
v                       v
调用Notion API      验证失败
users.me端点            |
|                       v
成功              返回错误信息
|
v
+---步骤2: 验证收入数据库---+
|                          |
v                          v
查询数据库信息         验证失败
|                          |
成功                         v
|                      返回错误信息
v
+---步骤3: 验证支出数据库---+
|                          |
v                          v
查询数据库信息         验证失败
|                          |
成功                         v
|                      返回错误信息
v
返回验证成功
```

---

## 相关文档

- [业务知识库/核心业务领域](./核心业务领域.md) - 业务概念
- [业务知识库/业务流程详解](./业务流程详解.md) - 详细流程说明
- [技术知识库/API索引](../技术知识库/API索引.md) - API端点
